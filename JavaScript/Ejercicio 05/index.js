let altura_cm = 175;
let apellido_m = 1.75;
let peso_kg = 75.50;

let redondeado = apellido_m.toFixed(0);
console.log(redondeado);

let peso_redondeado = peso_kg.toFixed(0) - 1;
console.log(peso_redondeado);

